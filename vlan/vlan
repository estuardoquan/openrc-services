#!/sbin/openrc-run

description="vlan"

depend() {
	need net
}

start() {
	ebegin "Adding VLANs"
	/usr/local/bin/vlan.sh -i "10 20" ens18
	eend $?
}

stop() {
	ebegin "Removing VLANs"
	/usr/local/bin/vlan.sh -d -i "10 20" ens18
	eend $?
}
